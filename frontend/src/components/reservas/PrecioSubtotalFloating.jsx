'use client';

import React from 'react';

/**
 * Este componente ha sido reemplazado por ReciboReservaGlobal
 * @description Se mantiene por compatibilidad pero no muestra nada en la UI
 */
const PrecioSubtotalFloating = () => {
  // Este componente ha sido reemplazado por ReciboReservaGlobal
  // Retornamos null para evitar que aparezca en la UI y cause conflictos
  return null;
};

export default PrecioSubtotalFloating;

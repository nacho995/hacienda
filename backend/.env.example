# Variables de Entorno del Backend (Ejemplo)
# Copia este archivo a .env y rellena con tus valores reales.
# ¡¡NO SUBAS EL ARCHIVO .env A GIT!!

# Base de Datos (Ejemplo MongoDB Atlas)
DATABASE_URL=mongodb+srv://tu_usuario:tu_password@tu_cluster.mongodb.net/tu_basedatos?retryWrites=true&w=majority

# Autenticación JWT
JWT_SECRET=tu_secreto_super_seguro_para_jwt
JWT_EXPIRES_IN=7d # O el tiempo que prefieras (ej: 1h, 30d)

# Configuración del Servidor
PORT=8080 # Elastic Beanstalk generalmente inyecta el puerto, pero puedes definir uno por defecto
NODE_ENV=production # Asegúrate de que esté en production para el deploy

# CORS (Añadir la URL de Vercel una vez desplegado el frontend)
CORS_ORIGIN=https://tu-proyecto.vercel.app # Reemplazar con tu URL de Vercel real

# Email (si usas Nodemailer/SendGrid, etc.) - Ejemplo SendGrid
# EMAIL_FROM=tu_email@dominio.com
# SENDGRID_API_KEY=tu_api_key_de_sendgrid 